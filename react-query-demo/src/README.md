# React Query Demo

This project demonstrates advanced data fetching and state management in React using React Query.

It is part of the ALX Front-End ReactJS program.

---

## Project Setup

This project was created using Vite and React.

To create the project:

```bash
npm create vite@latest react-query-demo -- --template react
cd react-query-demo
npm install
