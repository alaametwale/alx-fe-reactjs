# React Router Advanced

This project demonstrates advanced routing techniques in React using React Router v6.

It is part of the ALX Front-End ReactJS program.

---

## Project Setup

Create the project using Vite:

```bash
npm create vite@latest react-router-advanced -- --template react
cd react-router-advanced
npm install
